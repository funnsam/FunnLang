@import "*std/?/io"
@import "*libc/stdlib"

func pub main() int {
    for (i int: 0..9) {
        if ((i == 5) | (i == 7)) {
            putchar(i + 0x30);
            continue;
            putchar('!');
        } else if (i == 6) {
            putchar(i + 0x50);
            break;
            putchar('!');
        } else {
            putchar(i + 0x40);
            continue;
            putchar('!');
        }
    }
    return 0;
}
