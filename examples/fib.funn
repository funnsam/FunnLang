@import "*std/?/io"

func pub main() int {
    var a int = 1;
    var b int = 0;
    for (i int: 0..10) {
        a = b + a;
        b = a - b;
        // println(b);
        // TODO: Printing & Strings
    }
    return 0;
}
